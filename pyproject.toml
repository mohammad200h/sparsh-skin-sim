[project]
name = "sparsh-skin-sim"
version = "0.1.0"
description = ""
authors = [
    {name = "Mohammad",email = "mohammad200h@hotmail.com"}
]
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "mujoco (==3.11.0)",
    "matplotlib (>=3.11.1,<4.0.0)",
    "pytorch-kinematics (>=0.7.0,<1.0.0)",
    "torch (>=2.0.0,<3.0.0)",
    "einops (>=0.7.0,<1.0.0)",
    "open3d (>=0.18.0,<1.0.0)",
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
